import { EmptyHyphenPipe } from './empty-hyphen.pipe';

describe('EmptyHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
